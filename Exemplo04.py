#Gerar um número aleatorio no intervalo de 0.0 a 1.0

import random       #Essa linha importa o módulo random, que contém funções para gerar números aleatórios.

num = random.random() #A função random() do módulo random é chamada. Essa função retorna um número aleatório em ponto flutuante no intervalo de 0.0 a 1.0
print(num)